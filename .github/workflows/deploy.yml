name: Deploy Infrastructure

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Docker Context (PROD)
        run: |
          mkdir -p ~/.docker
          echo "${{ secrets.DOCKER_CA_PROD }}" | base64 -d > ~/.docker/ca.pem
          echo "${{ secrets.DOCKER_CERT_PROD }}" | base64 -d > ~/.docker/cert.pem
          echo "${{ secrets.DOCKER_KEY_PROD }}" | base64 -d > ~/.docker/key.pem
          chmod 600 ~/.docker/*
      
      - name: Deploy to Prod
        run: |
          docker context create prod --docker "host=tcp://172.16.170.139:2376,ca=~/.docker/ca.pem,cert=~/.docker/cert.pem,key=~/.docker/key.pem" || true
          docker context use prod
          docker stack deploy -c docker-compose.prod.yml shared-services-prod

  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Docker Context (DEV)
        run: |
          mkdir -p ~/.docker
          echo "${{ secrets.DOCKER_CA_DEV }}" | base64 -d > ~/.docker/ca.pem
          echo "${{ secrets.DOCKER_CERT_DEV }}" | base64 -d > ~/.docker/cert.pem
          echo "${{ secrets.DOCKER_KEY_DEV }}" | base64 -d > ~/.docker/key.pem
          chmod 600 ~/.docker/*
      
      - name: Deploy to Dev
        run: |
          docker context create dev --docker "host=tcp://172.16.170.138:2376,ca=~/.docker/ca.pem,cert=~/.docker/cert.pem,key=~/.docker/key.pem" || true
          docker context use dev
          docker stack deploy -c docker-compose.dev.yml shared-services-dev
